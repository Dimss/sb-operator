apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sbtest.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "sbtest.name" . }}
    helm.sh/chart: {{ include "sbtest.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "sbtest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
