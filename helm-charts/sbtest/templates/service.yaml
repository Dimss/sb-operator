apiVersion: v1
kind: Service
metadata:
  name: {{ include "sbtest.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "sbtest.name" . }}
    helm.sh/chart: {{ include "sbtest.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "sbtest.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
